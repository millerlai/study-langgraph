# 13.4 Chatbot 模擬與評估

## 目錄

1. [為什麼需要 Chatbot 模擬？](#1-為什麼需要-chatbot-模擬)
2. [聊天機器人模擬測試](#2-聊天機器人模擬測試)
3. [基準測試（Benchmarking）](#3-基準測試benchmarking)
4. [用 LangSmith 評估](#4-用-langsmith-評估)
5. [重點摘要](#5-重點摘要)
6. [參考資源](#6-參考資源)

---

## 1. 為什麼需要 Chatbot 模擬？

聊天機器人是多輪互動的應用。傳統的單次輸入/輸出評估無法覆蓋**多輪對話**中可能出現的問題：上下文遺失、重複回答、無法解決問題等。

```
傳統評估 vs 模擬評估

傳統（單輪）:
  輸入 ──> [Chatbot] ──> 輸出 ──> 評分
  ✗ 無法測試多輪對話
  ✗ 無法檢測上下文遺失

模擬（多輪）:
  ┌──────────────────────────────────┐
  │  模擬使用者                       │
  │  ┌────┐    ┌────────┐    ┌────┐ │
  │  │ Q1 │───>│Chatbot │───>│ A1 │ │
  │  │ Q2 │<───│        │<───│ A2 │ │
  │  │ Q3 │───>│        │───>│ A3 │ │
  │  │ .. │    │        │    │ .. │ │
  │  └────┘    └────────┘    └────┘ │
  │                                  │
  │  最終軌跡 ──> 評估器 ──> 分數    │
  └──────────────────────────────────┘
  ✓ 測試多輪對話能力
  ✓ 檢測上下文遺失與重複
  ✓ 端到端覆蓋
```

### 模擬評估的優勢

| 優勢 | 說明 |
|------|------|
| 容易上手 | 不需要預先建立完整的多輪對話資料集 |
| 端到端覆蓋 | 從第一個問題到解決（或未解決） |
| 檢測退化 | 能發現重複回答、上下文遺失等多輪問題 |

---

## 2. 聊天機器人模擬測試

### 核心概念

模擬測試需要兩個元件：

1. **app（你的應用）**：接收一則訊息，回傳一則回應
2. **user（模擬使用者）**：用 LLM 扮演使用者角色，根據對話歷史產生下一則提問

```
模擬架構

  ┌──────────────┐         ┌──────────────┐
  │ 模擬使用者    │  訊息   │  你的 App     │
  │ (LLM 扮演)   │────────>│ (Chatbot)    │
  │              │<────────│              │
  │ system:      │  回應    │ system:      │
  │ "你是一個    │         │ "你是客服"   │
  │  生氣的客戶" │         │              │
  └──────────────┘         └──────────────┘
         │                        │
         └────── 重複 N 輪 ───────┘
                    │
                    ▼
              [完整對話軌跡]
                    │
                    ▼
               [評估器評分]
```

### 範例：基本多輪模擬（純 LangGraph 實作）

```python
"""
基本多輪 Chatbot 模擬測試
不依賴外部模擬套件，純用 LangGraph 邏輯示範
"""
from typing import TypedDict, Annotated
from operator import add
from langgraph.graph import StateGraph, START, END


class Message(TypedDict):
    role: str
    content: str


class SimState(TypedDict):
    messages: Annotated[list[Message], add]
    turn: int
    max_turns: int


# === 模擬使用者 ===
SIMULATED_USER_QUERIES = [
    "我買的手機螢幕壞了，要退貨！",
    "我上週才買的，為什麼不能退？",
    "好吧，那你們可以修理嗎？",
    "修理要多久？",
    "可以接受，謝謝。",
]


def simulated_user(state: SimState) -> dict:
    turn = state["turn"]
    if turn < len(SIMULATED_USER_QUERIES):
        msg = {"role": "user", "content": SIMULATED_USER_QUERIES[turn]}
    else:
        msg = {"role": "user", "content": "好的，再見。"}
    return {"messages": [msg], "turn": turn + 1}


# === 你的 Chatbot ===
def chatbot(state: SimState) -> dict:
    last_msg = state["messages"][-1]["content"]

    # 簡單的規則式回應（實際場景中會呼叫 LLM）
    if "退貨" in last_msg or "退" in last_msg:
        reply = "很抱歉聽到您的問題。根據我們的政策，購買 7 天內可以退貨，請問您是什麼時候購買的？"
    elif "修理" in last_msg:
        reply = "可以的，我們提供免費保固維修服務。請您把手機寄到我們的服務中心。"
    elif "多久" in last_msg:
        reply = "一般維修需要 3-5 個工作天，我們會盡快處理。"
    elif "謝謝" in last_msg or "再見" in last_msg:
        reply = "不客氣！如有其他問題歡迎隨時聯繫我們。祝您有美好的一天！"
    else:
        reply = "我了解您的情況，讓我為您查看可能的解決方案。"

    return {"messages": [{"role": "assistant", "content": reply}]}


# === 路由：決定繼續或結束 ===
def should_continue(state: SimState) -> str:
    if state["turn"] >= state["max_turns"]:
        return "end"
    # 檢查最後一則訊息是否是結束語
    last_msg = state["messages"][-1]["content"]
    if "再見" in last_msg or "謝謝" in last_msg:
        return "end"
    return "continue"


# === 建構模擬圖 ===
graph = (
    StateGraph(SimState)
    .add_node("user", simulated_user)
    .add_node("chatbot", chatbot)
    .add_edge(START, "user")
    .add_edge("user", "chatbot")
    .add_conditional_edges(
        "chatbot",
        should_continue,
        {"continue": "user", "end": END},
    )
    .compile()
)

# === 執行模擬 ===
print("=== Chatbot 模擬測試 ===\n")
result = graph.invoke({
    "messages": [],
    "turn": 0,
    "max_turns": 5,
})

for msg in result["messages"]:
    role_label = "使用者" if msg["role"] == "user" else "客服  "
    print(f"  [{role_label}] {msg['content']}")

print(f"\n總共 {len(result['messages'])} 則訊息，{result['turn']} 輪對話")
```

**預期輸出：**
```
=== Chatbot 模擬測試 ===

  [使用者] 我買的手機螢幕壞了，要退貨！
  [客服  ] 很抱歉聽到您的問題。根據我們的政策，購買 7 天內可以退貨，請問您是什麼時候購買的？
  [使用者] 我上週才買的，為什麼不能退？
  [客服  ] 我了解您的情況，讓我為您查看可能的解決方案。
  [使用者] 好吧，那你們可以修理嗎？
  [客服  ] 可以的，我們提供免費保固維修服務。請您把手機寄到我們的服務中心。
  [使用者] 修理要多久？
  [客服  ] 一般維修需要 3-5 個工作天，我們會盡快處理。
  [使用者] 可以接受，謝謝。
  [客服  ] 不客氣！如有其他問題歡迎隨時聯繫我們。祝您有美好的一天！

總共 10 則訊息，5 輪對話
```

### 範例：使用 openevals 模擬（需安裝 openai + openevals）

```python
"""
使用 openevals 套件進行多輪模擬
（需安裝：pip install langsmith openevals openai）
（需設定：OPENAI_API_KEY, LANGSMITH_API_KEY）

注意：此範例需要真實的 API key 才能執行
"""
# from openevals.simulators import run_multiturn_simulation, create_llm_simulated_user
# from openevals.types import ChatCompletionMessage
# from langsmith.wrappers import wrap_openai
# from openai import OpenAI
#
# # 包裝 OpenAI 客戶端以啟用追蹤
# client = wrap_openai(OpenAI())
# history = {}
#
#
# # 你的應用邏輯
# def app(inputs: ChatCompletionMessage, *, thread_id: str, **kwargs):
#     if thread_id not in history:
#         history[thread_id] = []
#     history[thread_id].append(inputs)
#
#     res = client.chat.completions.create(
#         model="gpt-4.1-mini",
#         messages=[
#             {
#                 "role": "system",
#                 "content": "你是一個有耐心的客服人員，用繁體中文回答。",
#             },
#         ] + history[thread_id],
#     )
#     response_message = res.choices[0].message
#     history[thread_id].append(response_message)
#     return response_message
#
#
# # 建立模擬使用者
# user = create_llm_simulated_user(
#     system="你是一個憤怒的客戶，要求退掉你買的車。用繁體中文對話。",
#     model="openai:gpt-4.1-mini",
# )
#
# # 執行模擬
# result = run_multiturn_simulation(
#     app=app,
#     user=user,
#     max_turns=5,
# )
#
# # 印出對話軌跡
# for msg in result["trajectory"]:
#     role = "使用者" if msg["role"] == "user" else "客服"
#     print(f"[{role}] {msg['content']}")

# === 模擬輸出（示意） ===
print("=== openevals 模擬輸出（示意） ===")
print("[使用者] 這台車根本就是垃圾！我要求全額退款！")
print("[客服  ] 非常抱歉您有這樣的體驗。能否告訴我具體遇到什麼問題？")
print("[使用者] 引擎一直發出異響，才開了一個月就這樣！")
print("[客服  ] 我理解您的困擾。根據保固條款，我們可以先安排免費檢修...")
print("[使用者] 我不要修，我要退錢！")
print("[客服  ] 我了解您的訴求。讓我幫您轉接退貨部門的專員來處理...")
```

---

## 3. 基準測試（Benchmarking）

基準測試（Benchmarking）是在策劃的資料集上比較多個應用版本的表現。核心流程：建立資料集 -> 定義指標 -> 測試每個版本 -> 比較結果。

```
Benchmarking 流程

┌────────────────────────────────────────────┐
│              資料集 (Dataset)               │
│  ┌──────────────────────────────────────┐  │
│  │ Q: "什麼是 LangGraph？"              │  │
│  │ A: "圖狀態機框架"                     │  │
│  ├──────────────────────────────────────┤  │
│  │ Q: "什麼是 LangSmith？"              │  │
│  │ A: "LLM 可觀測性平台"                │  │
│  └──────────────────────────────────────┘  │
└──────────────┬─────────────────────────────┘
               │
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐
│版本 A  │ │版本 B  │ │版本 C  │
│GPT-4o  │ │GPT-4   │ │嚴格   │
│mini    │ │turbo   │ │prompt  │
└───┬────┘ └───┬────┘ └───┬────┘
    │          │          │
    ▼          ▼          ▼
┌────────────────────────────────┐
│         評估器 (Evaluators)     │
│  correctness + concision       │
└────────────────────────────────┘
    │          │          │
    ▼          ▼          ▼
  85%        90%        95%   <= 比較！
```

### 範例：本地基準測試

```python
"""
基準測試：比較不同版本的 Chatbot 表現
不需要 LangSmith，純本地執行
"""
from typing import TypedDict


# === 測試資料集 ===
dataset = [
    {"input": "什麼是 LangGraph？", "expected": "圖狀態機框架"},
    {"input": "什麼是 Python？",    "expected": "程式語言"},
    {"input": "什麼是 AI？",        "expected": "人工智慧"},
    {"input": "什麼是 REST API？",  "expected": "網路介面標準"},
    {"input": "什麼是 Docker？",    "expected": "容器化平台"},
]


# === 不同版本的 App ===
def app_v1(question: str) -> str:
    """簡短版"""
    answers = {
        "什麼是 LangGraph？": "一個框架",
        "什麼是 Python？": "程式語言",
        "什麼是 AI？": "人工智慧",
        "什麼是 REST API？": "API 標準",
        "什麼是 Docker？": "容器工具",
    }
    return answers.get(question, "不知道")


def app_v2(question: str) -> str:
    """詳細版"""
    answers = {
        "什麼是 LangGraph？": "LangGraph 是一個用於建構 AI 代理的圖狀態機框架，基於 LangChain 建構",
        "什麼是 Python？": "Python 是一種高階通用程式語言，以其簡潔的語法著稱",
        "什麼是 AI？": "AI 即人工智慧，是讓機器模擬人類智慧的技術",
        "什麼是 REST API？": "REST API 是一種遵循 REST 架構的網路介面標準",
        "什麼是 Docker？": "Docker 是一個開源的容器化平台，用於打包和部署應用程式",
    }
    return answers.get(question, "我不確定這個問題的答案")


# === 評估器 ===
def eval_correctness(response: str, expected: str) -> bool:
    """檢查回應是否包含預期的關鍵字"""
    return expected.lower() in response.lower() or response.lower() in expected.lower()


def eval_concision(response: str) -> float:
    """回應越短分數越高（最高 1.0）"""
    length = len(response)
    if length <= 10:
        return 1.0
    elif length <= 30:
        return 0.8
    elif length <= 60:
        return 0.5
    else:
        return 0.3


# === 執行基準測試 ===
def benchmark(app_fn, app_name: str):
    correct = 0
    concision_scores = []

    for item in dataset:
        response = app_fn(item["input"])
        is_correct = eval_correctness(response, item["expected"])
        concision = eval_concision(response)

        correct += int(is_correct)
        concision_scores.append(concision)

    accuracy = correct / len(dataset)
    avg_concision = sum(concision_scores) / len(concision_scores)
    return {
        "app": app_name,
        "accuracy": f"{accuracy:.0%}",
        "concision": f"{avg_concision:.2f}",
    }


# === 比較結果 ===
print("=== 基準測試結果 ===\n")
results = [
    benchmark(app_v1, "v1 (簡短)"),
    benchmark(app_v2, "v2 (詳細)"),
]

print(f"  {'版本':<15} {'正確率':<10} {'簡潔度':<10}")
print(f"  {'-'*35}")
for r in results:
    print(f"  {r['app']:<15} {r['accuracy']:<10} {r['concision']:<10}")
```

**預期輸出：**
```
=== 基準測試結果 ===

  版本             正確率      簡潔度
  -----------------------------------
  v1 (簡短)        60%        1.00
  v2 (詳細)        100%       0.38
```

---

## 4. 用 LangSmith 評估

LangSmith 提供完整的評估工作流，包括資料集管理、實驗追蹤、版本比較等功能。

```
LangSmith 評估流程

┌─────────────────────────────────────────────┐
│               LangSmith 平台                 │
│                                              │
│  1. 建立資料集                                │
│     client.create_dataset()                  │
│     client.create_examples()                 │
│                                              │
│  2. 定義評估器                                │
│     - LLM-as-Judge (用 LLM 判斷正確性)       │
│     - Code Evaluator (程式碼規則)             │
│     - Composite (加權組合)                    │
│                                              │
│  3. 執行實驗                                  │
│     client.evaluate(target, data, evaluators) │
│                                              │
│  4. 比較與追蹤                                │
│     - 實驗比較視圖                            │
│     - 時間序列趨勢                            │
│     - CI/CD 整合                              │
└─────────────────────────────────────────────┘
```

### 評估類型總覽

```
評估類型

離線評估 (Offline)              線上評估 (Online)
├── Benchmarking (基準測試)      ├── Real-time Monitoring
├── Unit Tests (單元測試)        ├── Anomaly Detection
├── Regression Tests (回歸測試)  └── Production Feedback
├── Backtesting (回測)
└── Pairwise (成對比較)
```

### 範例：使用 LangSmith evaluate()

```python
"""
LangSmith 評估流程（示意）
（需安裝：pip install langsmith openai）
（需設定：LANGSMITH_API_KEY, OPENAI_API_KEY）

注意：此範例需要真實的 API key 才能執行
以下用註解示範完整流程
"""

# === 完整的 LangSmith 評估流程 ===

# from langsmith import Client
# from langsmith.wrappers import wrap_openai
# import openai
#
# client = Client()
# openai_client = wrap_openai(openai.OpenAI())
#
# # 步驟 1：建立資料集
# dataset_name = "QA 評估資料集"
# dataset = client.create_dataset(dataset_name)
# client.create_examples(
#     dataset_id=dataset.id,
#     examples=[
#         {
#             "inputs": {"question": "什麼是 LangGraph？"},
#             "outputs": {"answer": "用於建構 AI 代理的圖狀態機框架"},
#         },
#         {
#             "inputs": {"question": "什麼是 LangSmith？"},
#             "outputs": {"answer": "LLM 應用的可觀測性和評估平台"},
#         },
#     ],
# )
#
# # 步驟 2：定義評估器
# eval_instructions = "你是評分專家，判斷預測答案是否正確。"
#
# def correctness(inputs: dict, outputs: dict, reference_outputs: dict) -> bool:
#     response = openai_client.chat.completions.create(
#         model="gpt-4.1-mini",
#         temperature=0,
#         messages=[
#             {"role": "system", "content": eval_instructions},
#             {"role": "user", "content": f"""
# 問題: {inputs['question']}
# 標準答案: {reference_outputs['answer']}
# 預測答案: {outputs['response']}
# 回答 CORRECT 或 INCORRECT:"""},
#         ],
#     ).choices[0].message.content
#     return response == "CORRECT"
#
# def concision(outputs: dict, reference_outputs: dict) -> bool:
#     return len(outputs["response"]) < 2 * len(reference_outputs["answer"])
#
# # 步驟 3：定義 target 函式
# def my_app(question: str) -> str:
#     return openai_client.chat.completions.create(
#         model="gpt-4.1-mini",
#         temperature=0,
#         messages=[
#             {"role": "system", "content": "用一句話簡短回答問題。"},
#             {"role": "user", "content": question},
#         ],
#     ).choices[0].message.content
#
# def target(inputs: dict) -> dict:
#     return {"response": my_app(inputs["question"])}
#
# # 步驟 4：執行評估
# results = client.evaluate(
#     target,
#     data=dataset_name,
#     evaluators=[correctness, concision],
#     experiment_prefix="gpt-4o-mini-v1",
# )
# print(f"實驗: {results.experiment_name}")

# === 本地模擬版本（不需要 API key） ===
print("=== LangSmith 評估流程模擬 ===\n")

# 模擬資料集
dataset = [
    {"inputs": {"question": "什麼是 LangGraph？"}, "outputs": {"answer": "圖狀態機框架"}},
    {"inputs": {"question": "什麼是 Python？"},    "outputs": {"answer": "程式語言"}},
    {"inputs": {"question": "什麼是 Docker？"},    "outputs": {"answer": "容器化平台"}},
]

# 模擬 target
def mock_target(inputs: dict) -> dict:
    answers = {
        "什麼是 LangGraph？": "LangGraph 是一個圖狀態機框架",
        "什麼是 Python？": "Python 是一種程式語言",
        "什麼是 Docker？": "Docker 是容器化平台",
    }
    return {"response": answers.get(inputs["question"], "不確定")}

# 模擬評估器
def mock_correctness(inputs, outputs, reference_outputs):
    return reference_outputs["answer"].lower() in outputs["response"].lower()

def mock_concision(outputs, reference_outputs):
    return len(outputs["response"]) < 3 * len(reference_outputs["answer"])

# 執行模擬評估
print(f"  {'問題':<20} {'正確性':<8} {'簡潔度':<8} {'回應'}")
print(f"  {'-'*70}")
for item in dataset:
    output = mock_target(item["inputs"])
    correct = mock_correctness(item["inputs"], output, item["outputs"])
    concise = mock_concision(output, item["outputs"])
    print(f"  {item['inputs']['question']:<20} {'PASS' if correct else 'FAIL':<8} "
          f"{'PASS' if concise else 'FAIL':<8} {output['response']}")

print("\n  總正確率: 100%")
print("  總簡潔度: 100%")
```

**預期輸出：**
```
=== LangSmith 評估流程模擬 ===

  問題                  正確性    簡潔度    回應
  ----------------------------------------------------------------------
  什麼是 LangGraph？    PASS     PASS     LangGraph 是一個圖狀態機框架
  什麼是 Python？       PASS     PASS     Python 是一種程式語言
  什麼是 Docker？       PASS     PASS     Docker 是容器化平台

  總正確率: 100%
  總簡潔度: 100%
```

### 範例：多輪模擬 + LangSmith 評估整合

```python
"""
多輪模擬 + LangSmith 評估整合（使用 pytest）
（需安裝：pip install langsmith openevals openai pytest）

注意：此範例需要真實的 API key 才能執行
以下用註解示範完整流程
"""

# import pytest
# from openevals.simulators import run_multiturn_simulation, create_llm_simulated_user
# from openevals.llm import create_llm_as_judge
# from openevals.types import ChatCompletionMessage
# from langsmith import testing as t
# from langsmith.wrappers import wrap_openai
# from openai import OpenAI
#
#
# @pytest.mark.langsmith
# def test_customer_service_simulation():
#     """測試客服 Chatbot 的多輪對話表現"""
#     inputs = {"role": "user", "content": "我想退貨"}
#     t.log_inputs(inputs)
#
#     client = wrap_openai(OpenAI())
#     history = {}
#
#     def app(inputs: ChatCompletionMessage, *, thread_id: str, **kwargs):
#         if thread_id not in history:
#             history[thread_id] = []
#         history[thread_id].append(inputs)
#         res = client.chat.completions.create(
#             model="gpt-4.1-mini",
#             messages=[
#                 {"role": "system", "content": "你是一個有耐心的客服人員。"},
#             ] + history[thread_id],
#         )
#         response = res.choices[0].message
#         history[thread_id].append(response)
#         return response
#
#     user = create_llm_simulated_user(
#         system="你是一個禮貌的客戶，想退掉你買的耳機。",
#         model="openai:gpt-4.1-mini",
#         fixed_responses=[inputs],
#     )
#
#     # 定義軌跡評估器
#     trajectory_evaluator = create_llm_as_judge(
#         model="openai:o3-mini",
#         prompt="根據以下對話，客戶是否滿意？\n{outputs}",
#         feedback_key="satisfaction",
#     )
#
#     # 執行模擬
#     res = run_multiturn_simulation(
#         app=app,
#         user=user,
#         trajectory_evaluators=[trajectory_evaluator],
#         max_turns=5,
#     )
#     t.log_outputs(res)
#
#     # 斷言：評估器應判斷客戶滿意
#     assert res["evaluator_results"][0]["score"]

# === 本地模擬版 ===
print("=== pytest + 多輪模擬 整合示意 ===")
print()
print("執行命令: pytest test_chatbot.py -v")
print()
print("預期輸出:")
print("  test_chatbot.py::test_customer_service_simulation PASSED")
print("  LangSmith 實驗 URL: https://smith.langchain.com/...")
print("  satisfaction score: True")
```

---

## 5. 重點摘要

| 主題 | 說明 | 關鍵工具 / API |
|------|------|----------------|
| 多輪模擬 | 用 LLM 扮演使用者進行多輪對話測試 | `openevals.simulators` |
| 模擬使用者 | 定義使用者角色和行為的 system prompt | `create_llm_simulated_user()` |
| 固定起始訊息 | 用 `fixed_responses` 確保第一輪訊息固定 | `fixed_responses=[...]` |
| 軌跡評估 | 對完整對話軌跡進行端到端評估 | `trajectory_evaluators` |
| 基準測試 | 在固定資料集上比較多個版本的表現 | `client.evaluate()` |
| LLM-as-Judge | 用 LLM 判斷回答正確性等主觀指標 | `create_llm_as_judge()` |
| Code Evaluator | 用程式碼規則檢查結構化條件 | 自定義 Python 函式 |
| CI/CD 整合 | 用 pytest 在 CI 中自動執行評估 | `@pytest.mark.langsmith` |
| 實驗追蹤 | 在 LangSmith UI 中比較和追蹤實驗結果 | LangSmith Dashboard |

---

## 6. 參考資源

- [LangSmith: How to simulate multi-turn interactions](https://docs.smith.langchain.com/evaluation/how_to_guides/simulate-multi-turn)
- [LangSmith: Evaluate a chatbot](https://docs.smith.langchain.com/evaluation/tutorials/evaluate-chatbot)
- [LangSmith: Evaluation types](https://docs.smith.langchain.com/evaluation/concepts/evaluation-types)
- [OpenEvals GitHub](https://github.com/langchain-ai/openevals)
- [LangSmith Evaluation Quickstart](https://docs.smith.langchain.com/evaluation/quickstart)
- [LangGraph + LangSmith 整合](https://langchain-ai.github.io/langgraph/how-tos/monitoring/)
